# SiuBarber Admin

Admin dashboard runs as a separate Next.js app from the public website.

## Development

```bash
pnpm install
pnpm dev
```

Default local URL:

```txt
http://localhost:5144
```

The app owns its layout, styles, components, modules, and API client. Do not import layout/UI/style files from `apps/web`.
